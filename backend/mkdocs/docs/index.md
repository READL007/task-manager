# Task Manager API

This documentation describes the backend of the Task Manager project, built with **FastAPI**, **JS**, and **SQLite**.

The API exposes CRUD operations for tasks, YAML import, admin endpoints, and health/debug utilities.

Version: **1.0.1**
