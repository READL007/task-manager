# Architecture

The backend follows a clean, modular FastAPI structure:

```
app/
├── main.py           # FastAPI application and routes
├── db.py             # Database
├── models.py         # ORM models
├── schemas.py        # Pydantic schemas
└── ...

```
